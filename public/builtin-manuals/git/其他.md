## 其他常用命令

一些不属于某个特定类别但日常开发中很常用的 Git 命令。

```bash
# 查看git版本
git --version

# 清除本地git缓存，常用于 .gitignore 修改后刷新追踪
git rm -r --cached .

# 列出没有被 .gitignore 忽略的文件列表
git ls-files

# 列出被忽略的文件
git ls-files --others --ignored --exclude-standard

# 统计代码行数（按文件类型）
git ls-files | xargs wc -l
```
